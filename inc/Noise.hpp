#pragma once

#include "Maths.hpp"

namespace maths::random::noise
{

enum class NoiseType : int
{

};

class Noise
{
private:
    /* data */
public:
    Noise(/* args */);
    ~Noise();
};
    
} // namespace maths::random::noise
