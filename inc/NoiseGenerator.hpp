#pragma once

#include "Maths.hpp"

namespace maths::random::noise
{

enum class NoiseType : int
{

};

class NoiseGenerator
{
private:
    /* data */
public:
    NoiseGenerator(/* args */);
    ~NoiseGenerator();
};
    
} // namespace maths::random::noise
